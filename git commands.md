* git clone 

* ls:list
* cd:change dir
* git branch --all (to see all branches)
* git checkout siva
* git status
* git add .  
* git add  <file\_name>
* git commit  -m "....."
* git push (from local folders to git app)

* git pull (to takes files from git app to local folders)
